
from GUI import GUI_Window


if __name__ == '__main__':
    GUI = GUI_Window()
    GUI.set_mode('test')

    # For program testing purposes, execute the command:
    # GUI.set_mode('test')

    # For launch or test fire purposes, execute the command:
    # GUI.set_mode('launch')
